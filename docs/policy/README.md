# Policy Documents

This directory stores configurable rules, defaults, and versioned runtime policies.

Policy documents are allowed to change across versions, but they must not rewrite stable SDS behavior definitions or Foundation principles.

